#pragma include simple-vf

#if defined VS
attribute vec3 Apos;
#endif

uniform mat4 Umvp;

varying float Vid;

#if defined VS
vec4 vertex () {
    vec4 position = Umvp * vec4 (Apos, 1.0);
	Vid = gl_VertexID;
	return position;
}
#elif defined FS
vec3 fragment () {
	return vec3(Vid / 36);
}
#endif

