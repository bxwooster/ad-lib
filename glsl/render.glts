#if defined VS
in vec4 pos;
#endif

uniform mat4 Umvp;

#if defined VS
void main(void) {
    vec4 position = Umvp * pos;
	gl_Position = position;
}
#elif defined FS
void main(void) {
	gl_FragColor = vec4 (1);
}
#endif

